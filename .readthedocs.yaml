version: 2

build:
  os: ubuntu-20.04
  tools:
    python: "3.10"
  commands:
    - pip install uv==0.10.11
    - uv sync --group docs
    - uv run sphinx-build -b html docs docs/build
    - mkdir -p _readthedocs
    - mv docs/build _readthedocs/html
